Realiza los siguientes tests del componente Formulario.jsx, todos dentro del fichero Formulario.test.js que 
alojaras en la carpeta tests dentro de src. Usa los describe() para organizar los tests recuerda importar tanto
el componente formulario como la funcion mayorDeEdad en el fichero de test

a ) test unitarios de la funcion mayorDeEdad() que esta dentro del componente Formulario.jsx:
-> Comprobar que la funcion devuelve un valor Boolean
-> Comprobar que si la entrada a la funcion es 18 devuelve true
-> Comprobar que si la entrada a la funcion es un numero mayor a 18 nos devuelve true
-> Comprobar que si la entrada a la funcion es un numero menor a 18 nos devuelve false
-> Comprobar que si la entrada a la funcion es un numero negativo nos devuelve null

b) test de funcionalidad del formulario
-> Comprueba que se renderizan los siguientes elementos: la cabacera cuyo contenido es "rellena el formulario",
la imagen del gato, el campo de texto del nombre, el campo de texto de la edad y el boton.
-> comprueba que cuando el usuario rellena el formulario aparece una cabecera de nivel 6 en el formulario